before_script:
  - mkdir -p ~/.ssh
  - chmod 700 ~/.ssh

  - echo "$SSH_PRIVATE_KEY" | tr -d '\r' > ~/.ssh/id_ed25519
  - chmod 600 ~/.ssh/id_ed25519

  - ssh-keyscan -H $SFTP_HOST >> ~/.ssh/known_hosts

  - ssh -i ~/.ssh/id_ed25519 -o BatchMode=yes vanni@$SFTP_HOST "echo OK"