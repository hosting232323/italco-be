stages:
  - test

image: python:3.11

services:
  - name: postgres:15
    alias: postgres

variables:
  POSTGRES_USER: myuser
  DATABASE_URL: postgresql://${POSTGRES_USER}:mypassword@postgres:5432/mydb
  PYTHONUNBUFFERED: 1

before_script:
  - apt-get update && apt-get install -y postgresql-client
  - pip install --upgrade pip
  - pip install -r requirements.txt
  - export DATABASE_URL=$DATABASE_URL

backend_test:
  stage: test
  script:
    - until pg_isready -h postgres -U $POSTGRES_USER; do echo "Waiting for postgres..."; sleep 2; done
    - alembic upgrade heads
    - python -m src &
    - until curl -s http://127.0.0.1:8080; do echo "Waiting for backend..."; sleep 2; done
    - curl -v http://127.0.0.1:8080/
