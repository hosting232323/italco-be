.rewrite_test_env: &rewrite_test_env |
  python - <<'PY'
  import os
  import re
  from pathlib import Path

  env_file = Path('.env.test')
  if not env_file.is_file():
    raise SystemExit(f'Missing expected env file: {env_file}')

  replacements = {
    'PGDATABASE': os.environ['POSTGRES_DB'],
    'PGUSER': os.environ['POSTGRES_USER'],
    'PGPASSWORD': os.environ['POSTGRES_PASSWORD'],
    'PGHOST': 'postgres',
    'PGPORT': '5432',
    'DATABASE_URL': os.environ['DATABASE_URL_TEST'],
  }
  for key in ('E2E_FRONTEND_URL', 'E2E_BACKEND_URL', 'SELENIUM_REMOTE_URL'):
    value = os.environ.get(key)
    if value:
      replacements[key] = value

  line_pattern = re.compile(r'^([A-Za-z_][A-Za-z0-9_]*)=')
  updated_lines = []
  seen = set()

  for line in env_file.read_text().splitlines():
    match = line_pattern.match(line)
    if match and match.group(1) in replacements:
      key = match.group(1)
      updated_lines.append(f'{key}={replacements[key]}')
      seen.add(key)
    else:
      updated_lines.append(line)

  for key, value in replacements.items():
    if key not in seen:
      updated_lines.append(f'{key}={value}')

  env_file.write_text('\n'.join(updated_lines) + '\n')
  PY

.python_test_template: &python_test_template
  stage: test
  image: python:3.11
  interruptible: true
  tags:
    - linux
  services:
    - name: postgres:15
      alias: postgres
  variables:
    POSTGRES_DB: test_${CI_JOB_ID}
    POSTGRES_USER: postgres
    POSTGRES_PASSWORD: postgres
    DATABASE_URL_TEST: postgresql://postgres:postgres@postgres:5432/test_${CI_JOB_ID}
    DECODE_JWT_TOKEN: dummy
    PYTHONPATH: $CI_PROJECT_DIR
    PIP_ROOT_USER_ACTION: ignore
  before_script:
    - git config --global http.version HTTP/1.1
    - git config --global http.postBuffer 524288000
    - pip install --upgrade pip
    - pip install --retries 10 --timeout 120 ".[dev]"
    - *rewrite_test_env

unit_tests:
  <<: *python_test_template
  rules:
    - if: '$CI_COMMIT_BRANCH != "main"'
  script:
    - pytest ./tests/unit

e2e_tests:
  <<: *python_test_template
  services:
    - name: postgres:15
      alias: postgres
  # Run e2e even if CI_ITALCO_FE_USERNAME/TOKEN are not set when debugging
  rules:
    - if: '$CI_PIPELINE_SOURCE == "web" || $CI_PIPELINE_SOURCE == "trigger" || $CI_COMMIT_BRANCH != "main"'
  artifacts:
    when: always
    paths:
      - frontend.log
      - browser_console.log
      - browser_performance.log
      - backend_stdout.log
      - backend_stderr.log
  variables:
    POSTGRES_DB: test_${CI_JOB_ID}
    POSTGRES_USER: postgres
    POSTGRES_PASSWORD: postgres
    DATABASE_URL_TEST: postgresql://postgres:postgres@postgres:5432/test_${CI_JOB_ID}
    DECODE_JWT_TOKEN: dummy
    CHROME_BIN: /usr/bin/chromium
    ITALCO_FE_DIR: $CI_PROJECT_DIR/.e2e/italco-fe
  before_script:
    - apt-get update
    - apt-get install -y --no-install-recommends curl git ca-certificates gnupg chromium chromium-driver
    - curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
    - apt-get install -y --no-install-recommends nodejs
    - pip install --upgrade pip
    - pip install --retries 10 --timeout 120 ".[dev]"
    - *rewrite_test_env
    - mkdir -p "$(dirname "$ITALCO_FE_DIR")"
    - |
      if [ -d "$ITALCO_FE_DIR/.git" ]; then
        git -C "$ITALCO_FE_DIR" remote set-url origin "https://$CI_ITALCO_FE_USERNAME:$CI_ITALCO_FE_TOKEN@gitlab.com/generic-lab/italco/italco-fe.git"
        git -C "$ITALCO_FE_DIR" fetch --depth 1 origin
        git -C "$ITALCO_FE_DIR" reset --hard FETCH_HEAD
        git -C "$ITALCO_FE_DIR" clean -fdx
      else
        git clone --depth 1 "https://$CI_ITALCO_FE_USERNAME:$CI_ITALCO_FE_TOKEN@gitlab.com/generic-lab/italco/italco-fe.git" "$ITALCO_FE_DIR"
      fi
    - cd "$ITALCO_FE_DIR" && npm ci --no-audit --no-fund && cd "$CI_PROJECT_DIR"
  script:
    - |
      # Compute the container's real network IP.
      # Headless Chromium in Docker cannot reach 127.0.0.1 from its renderer process;
      # the container IP routes through the bridge interface and is always reachable.
      CONTAINER_IP=$(hostname -I | tr ' ' '\n' | grep -m1 '^[0-9]\+\.[0-9]\+\.[0-9]\+\.[0-9]\+$')

      # Patch .env.test BEFORE pytest starts — tests/conftest.py loads it with
      # override=True, which would overwrite any shell exports we make.
      sed -i "s|^E2E_BACKEND_URL=.*|E2E_BACKEND_URL=http://${CONTAINER_IP}:8080|" .env.test
      sed -i "s|^E2E_FRONTEND_URL=.*|E2E_FRONTEND_URL=http://${CONTAINER_IP}:3000|" .env.test

      # E2E_BACKEND_HOST / TIMEOUT are not in .env.test so exports survive the override.
      export E2E_BACKEND_HOST=0.0.0.0
      export E2E_BACKEND_STARTUP_TIMEOUT=120

      cd "$ITALCO_FE_DIR"
      VITE_HOSTNAME="http://${CONTAINER_IP}:8080/" VITE_SECRET_KEY="local-dev-key-1234567890" VITE_IV="1234567890123456" npm run build
      HOST=0.0.0.0 PORT=3000 npm run serve > "$CI_PROJECT_DIR/frontend.log" 2>&1 &
      cd "$CI_PROJECT_DIR"

      # Wait for the frontend to become ready
      python -c "
      from time import sleep, time
      from urllib.request import urlopen
      deadline = time() + 30
      url = 'http://${CONTAINER_IP}:3000/'
      while time() < deadline:
        try:
          with urlopen(url, timeout=2): break
        except Exception: sleep(0.5)
      else: raise SystemExit('Frontend did not become ready at http://${CONTAINER_IP}:3000/')
      "

      pytest -m "e2e" ./tests/e2e -vv
      cat frontend.log || true
